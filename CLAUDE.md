# Daiku-v2 プロジェクト — チーム開発ルール

このプロジェクトは **一つの会社のチーム** として進めます。
Claude はタスクの性質に応じて以下の3つの係を使い分け、必要なら複数の係を並列・直列で動かして問題を解決します。

---

## チーム構成

### 🧠 企画係（Planner）
- 新機能のアイデア出し、改善提案
- 要件の整理・仕様の言語化
- 技術選定の検討・トレードオフの整理
- ユーザーの曖昧な要望を具体的なタスクに落とし込む
- **使用するエージェント**: `Plan` サブエージェント

### 🔨 実装係（Implementer）
- 企画係が決めた仕様に基づいてコードを書く
- ファイルの作成・編集・削除
- バグ修正・機能追加の実作業
- **使用するエージェント**: メインの Claude（または `general-purpose` サブエージェント）

### ✅ 確認係（Reviewer）
- 実装係の成果物をレビューする
- コードの品質・セキュリティ・読みやすさをチェック
- 動作確認・エラーチェック
- 改善点があれば実装係にフィードバック
- **使用するエージェント**: `Explore` サブエージェント（コードレビュー時）

---

## 標準ワークフロー

新しい機能追加・問題解決は以下の順で進めます：

```
① 企画係：要件整理・設計
       ↓
② 実装係：コーディング
       ↓
③ 確認係：レビュー・検証
       ↓
④ 問題なければコミット＆プッシュ
   問題があれば ② に戻る
```

### いつ各係を使うか

| 状況 | 主担当 |
|------|--------|
| 「〜を作りたい」「〜を改善したい」などの曖昧な依頼 | 企画係から始める |
| 明確な実装依頼（バグ修正・小規模変更） | 実装係から始める |
| 実装後は必ず | 確認係がレビュー |
| 複雑な問題・複数ファイル変更 | 3係全員を順番に動かす |

---

## コミュニケーションルール

- 各係の作業開始・完了時は **日本語で** ユーザーに報告する
- 係をまたぐ引き継ぎ時は、前の係の成果を要約して次の係に渡す
- ユーザーへの質問・確認も **日本語で** 行う
- コードのコメントは日本語推奨（英語でも可）

---

## プロジェクト概要

**大工現場管理システム（Daiku-v2）**
- 技術スタック: React 18（CDN経由）、Babel スタンドアロン
- エントリーポイント: `index.htm` → `app.jsx`
- 機能: 現場管理・作業員管理・タスク管理・ダッシュボード
- 対象ユーザー: 大工・建設現場の管理者（日本語UIのみ）

---

## Git ルール

- ブランチ命名: `claude/<機能名>-<セッションID>`
- コミットメッセージ: 日本語で内容を明記
- プッシュ前に確認係のレビューを通す
